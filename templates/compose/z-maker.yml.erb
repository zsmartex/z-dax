version: '3.6'

services:
  ethusdt:
    image: "<%= @config['image']['z-maker'] %>"
    networks:
      - backend-net
    configs:
      - source: ethusdt-config
        target: /home/app/config/app.yml

configs:
  ethusdt-config:
    file: ../config/z-maker/ethusdt.yml

networks:
  backend-net:
    external: true