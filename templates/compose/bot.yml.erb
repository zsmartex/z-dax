version: '3.8'

services:
  contrive:
    image: "<%= @config['images']['contrive'] %>"
    env_file:
      - ../config/contrive.env
    volumes:
      - ../config/contrive/strategies.yml:/app/config/strategies.yml
    command: start
