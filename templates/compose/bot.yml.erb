version: '3.8'

services:
  quantex:
    image: "<%= @config['images']['quantex'] %>"
    restart: always
    env_file:
      - ../config/quantex.env
    volumes:
      - ../config/quantex/config.yml:/app/config/config.yml
    command: "./quantex"
