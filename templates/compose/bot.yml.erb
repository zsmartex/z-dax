version: '3.8'

services:
  quantex-api:
    image: "<%= @utils['images']['quantex'] %>"
    restart: always
    env_file:
      - ../config/quantex.env
    command: "./quantex-api"

  quantex-engine:
    image: "<%= @utils['images']['quantex'] %>"
    restart: always
    env_file:
      - ../config/quantex.env
    command: "./quantex-engine"
