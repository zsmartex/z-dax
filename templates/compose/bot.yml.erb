version: '3.8'

services:
  contrive:
    image: "<%= @config['image']['contrive'] %>"
    env_file:
      - ../config/contrive.env
    volumes:
      - ../config/contrive/strategies.yml:/app/config/strategies.yml
    command: start
