version: '3.8'

services:
  quantex:
    image: "<%= @utils['images']['quantex'] %>"
    restart: always
    env_file:
      - ../config/quantex.env
    volumes:
      - ../config/quantex/strategies.yaml:/app/config/strategies.yaml
    command: "./quantex"
