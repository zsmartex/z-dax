version: '3.8'

services:
  contrive:
    build:
      context: "../vendor/contrive"
    restart: always
    env_file:
      - ../config/contrive.env
    volumes:
      - ../config/contrive/strategies.yml:/app/config/strategies.yml
    command: start
