version: '3.8'

services:
  quantex:
    image: "<%= @utils['images']['quantex'] %>"
    restart: always
    env_file:
      - ../config/quantex.env
    volumes:
      - ../config/quantex/config.yml:/app/config/config.yml
    command: "./quantex"
