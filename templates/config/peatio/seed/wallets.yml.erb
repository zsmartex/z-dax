- name:             Ethereum Deposit Wallet
  blockchain_key:   eth-goerli
  currency_id:      eth
  # Address where deposits will be collected to.
  address:          '0xeaCB530262a39438579968bd3877523B3B4c23f2'  # IMPORTANT: Always wrap this value in quotes!
  kind:             deposit  # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      0.0
  status:           active
  gateway:          geth     # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Ethereum Hot Wallet
  blockchain_key:   eth-goerli
  currency_id:      eth
  # Address where deposits will be collected to.
  address:          '0x671eb69873f1D762bDCE3aA52649b4B82bb29766'  # IMPORTANT: Always wrap this value in quotes!
  kind:             hot     # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      100.0
  status:           active
  gateway:          geth    # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Ethereum Warm Wallet
  blockchain_key:   eth-goerli
  currency_id:      eth
  # Address where deposits will be collected to.
  address:          '0x130e74084924A5e2391E872317f70473897bD87e'  # IMPORTANT: Always wrap this value in quotes!
  kind:             warm    # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      1000.0
  status:           active
  gateway:          geth    # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Tether USD Deposit Wallet
  blockchain_key:   eth-goerli
  currency_id:      usdt
  # Address where deposits will be collected to.
  address:          '0xeaCB530262a39438579968bd3877523B3B4c23f2'  # IMPORTANT: Always wrap this value in quotes!
  kind:             deposit  # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      0.0
  status:           active
  gateway:          geth     # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Tether USD Hot Wallet
  blockchain_key:   eth-goerli
  currency_id:      usdt
  # Address where deposits will be collected to.
  address:          '0x671eb69873f1D762bDCE3aA52649b4B82bb29766'  # IMPORTANT: Always wrap this value in quotes!
  kind:             hot     # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      100.0
  status:           active
  gateway:          geth    # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Tether USD Warm Wallet
  blockchain_key:   eth-goerli
  currency_id:      usdt
  # Address where deposits will be collected to.
  address:          '0x130e74084924A5e2391E872317f70473897bD87e'  # IMPORTANT: Always wrap this value in quotes!
  kind:             warm    # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      1000.0
  status:           active
  gateway:          geth    # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Goerli Test Token Deposit Wallet
  blockchain_key:   eth-goerli
  currency_id:      tst
  # Address where deposits will be collected to.
  address:          '0xeaCB530262a39438579968bd3877523B3B4c23f2'  # IMPORTANT: Always wrap this value in quotes!
  kind:             deposit  # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      0.0
  status:           active
  gateway:          geth     # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Goerli Test Token Hot Wallet
  blockchain_key:   eth-goerli
  currency_id:      tst
  # Address where deposits will be collected to.
  address:          '0x671eb69873f1D762bDCE3aA52649b4B82bb29766'  # IMPORTANT: Always wrap this value in quotes!
  kind:             hot     # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      100.0
  status:           active
  gateway:          geth    # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Goerli Test Token Warm Wallet
  blockchain_key:   eth-goerli
  currency_id:      tst
  # Address where deposits will be collected to.
  address:          '0x130e74084924A5e2391E872317f70473897bD87e'  # IMPORTANT: Always wrap this value in quotes!
  kind:             warm    # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      1000.0
  status:           active
  gateway:          geth    # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Zippie Deposit Wallet
  blockchain_key:   eth-goerli
  currency_id:      zipt
  # Address where deposits will be collected to.
  address:          '0xeaCB530262a39438579968bd3877523B3B4c23f2'  # IMPORTANT: Always wrap this value in quotes!
  kind:             deposit  # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      0.0
  status:           active
  gateway:          geth     # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Zippie Hot Wallet
  blockchain_key:   eth-goerli
  currency_id:      zipt
  # Address where deposits will be collected to.
  address:          '0x671eb69873f1D762bDCE3aA52649b4B82bb29766'  # IMPORTANT: Always wrap this value in quotes!
  kind:             hot     # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      100.0
  status:           active
  gateway:          geth    # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'

- name:             Zippie Warm Wallet
  blockchain_key:   eth-goerli
  currency_id:      zipt
  # Address where deposits will be collected to.
  address:          '0x130e74084924A5e2391E872317f70473897bD87e'  # IMPORTANT: Always wrap this value in quotes!
  kind:             warm    # Wallet kind (deposit, hot, warm, cold or fee).
  max_balance:      1000.0
  status:           active
  gateway:          geth    # Gateway client name.
  settings:
    #
    # Geth gateway client settings.
    uri:          http://geth:8545
    secret:       'exchangewallet'